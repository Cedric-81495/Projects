node_modules
.next
out
qr-out
*.tsbuildinfo
next-env.d.ts
.DS_Store
.vercel

# Secrets. `.env*` catches every real env file; the two exceptions below are
# templates containing no credentials and MUST be committed — without them a
# new machine has nothing to copy from.
.env*
!.env.example
!.env.local.example

# Working notes — kept locally, not tracked. Added 2026-09-08 after they were
# staged by accident: GWOP-CONTEXT.md maps named team members to roles and was
# last verified Aug 22, so it is stale as well as personal.
GWOP-CONTEXT.md
GWOP-INSTRUCTIONS.md
INSTALL.md
PRE-PUSH-REVIEW.md
