# Base URL for the content API (used when VITE_USE_API=true).
VITE_API_BASE=/api

# Data source switch.
#   false (default) -> serve the typed local seed content in src/data
#   true            -> read from VITE_API_BASE; falls back to seed on error
VITE_USE_API=false
